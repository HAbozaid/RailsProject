<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
	<%= gravatar_for user, size: 50 %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>